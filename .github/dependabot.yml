# workflow 里引用的 action 靠 dependabot 跟版本：人肉盯着 actions/checkout 发新版不现实。
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
# 刻意不加 swift 生态：CopyoCore 是工程内的本地包，零远程 SPM 依赖、也没有
# Package.resolved，加了只会每月空转一次。
